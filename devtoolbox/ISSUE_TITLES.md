# Issue Ideas (v1 sprint)

Format: Title — Labels

1) Add `--indent` option to `fmt-json` — type:feature, good first issue
2) Add `--sort-keys/--no-sort-keys` to `fmt-json` — type:feature
3) Support reading JSON from `.jsonl` and format each line — type:feature
4) `slugify` should preserve unicode when `--ascii-only` is false — type:feature
5) Add `--separator` option to `slugify` (default `-`) — type:feature, good first issue
6) `extract-links` should support `mailto:` and `ftp:` — type:feature
7) Add `--unique/--no-unique` to `extract-links` — type:feature, good first issue
8) `batch-rename` add `--padding` (e.g., 001, 002) — type:feature
9) `batch-rename` support filtering by extension — type:feature, good first issue
10) `md-to-pdf` add `--css` option to pass a custom stylesheet — type:feature
11) `md-to-pdf` support `--title` override — type:feature
12) `img-resize` add `--format` option to change output format — type:feature
13) `img-resize` allow recursive folder traversal — type:feature
14) Improve error messages for missing dependencies — type:docs
15) Add `--version` output to CLI — type:feature

