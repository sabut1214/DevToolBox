# Dev Toolbox v1.0.0

## Highlights
- Core CLI with 6 subcommands: `fmt-json`, `slugify`, `extract-links`, `batch-rename`, `md-to-pdf`, `img-resize`
- Typer-based UX with built-in help
- Test suite + CI workflow

## Install

```bash
pipx install devtoolbox
```

## Usage

```bash
dt --help
```
